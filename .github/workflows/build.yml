name: Build VAZ APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Build APK Cleanly
        run: |
          # Создаем чистый проект
          flutter create --platforms=android --org com.vaz.app vaz_app
          cd vaz_app
          
          # Добавляем надежный звуковой плагин
          flutter pub add audioplayers
          
          # Подменяем код на наш
          cp ../lib/main.dart lib/main.dart
          
          # Собираем APK
          flutter build apk --release

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: vaz-turbo-apk
          path: vaz_app/build/app/outputs/flutter-apk/app-release.apk
